<%#partial for displaying a comment%>

<p>
  <strong>Commenter:</strong>
  <%= comment.commenter %>
</p>

<p>
  <strong>Comment:</strong>
  <%= comment.body %>
</p>

<%#link to destroy comment%>
<p>
    <%#fire off a DELETE /articles/:article_id/comments/:id to our CommentsController, which can use this to find comment we want to delete%>
    <%#need to pass both the article of this comment, and comment itself to controller, because it uses article to fetch the comment to destroy%>
  <%= link_to 'Destroy Comment', [comment.article, comment],
               method: :delete,
               data: { confirm: "Are you sure?" } %>
</p>
